# v0.2.0 发布后集中清理记录

基线为发布标签 v0.2.0（0739df8）。这些是本地后续修改，不回写已发布附件。

## 已处理

- 退役设置接口内约 500 行旧构建、版本改写、Release 删除和强制标签逻辑，保留 HTTP 410 兼容提示；页面相关状态和按钮同步移除。
- 本地构建集中到 scripts/build_desktop.py，旧 bat/ps1/sh 为薄包装；始终先检查后端版本和健康状态，禁止本地自动发布或单机混打多平台。
- CI、Docker 和本地统一从 pyproject.toml 的 desktop extra 获取运行依赖；去除重复列表和本地 prompts 的强制 wheel 包含。
- Docker 构建上下文改为默认拒绝的白名单；桌面构建不再隐式收集本地忽略的 prompts/skills，原始文件保留。
- 四个真实模型脚本移到 scripts/diagnostics/，更新路径并单独说明收费与副作用边界；没有运行真实模型验收。
- 旧 token URL 推送脚本和内部交接文件原样归档到本机 maintenance/archive/2026-09-17-post-release/。
- 启动、图标生成脚本去除固定盘符；Python 最低版本提示与项目声明同步。
- 中英文 README 收敛为当前功能、真实入口、构建方法与目录导航；历史发布诊断明确标为历史，维护索引同步 v0.2.0 发布结果。

## 验证

- Python 回归：380 项通过，一个原有 tar 解压弃用警告。
- 桌面启动逻辑：5 项 Node 测试通过。
- 内联页面全部脚本完成 JSX 语法解析；保留组件树 TypeScript noEmit 检查通过。
- 一致性检查、PowerShell 包装器解析、安装包配置及诊断脚本编译检查通过。
- Python wheel 构建通过，检查 121 个条目，未夹带凭据、归档或 node_modules。
- 新冻结后端构建及真实启动检查通过：隔离中文空格目录、无密钥健康接口、HTML、favicon、品牌资源和 0.2.0 版本。
- Docker daemon 未启动，未运行完整镜像构建；未重发安装包、未做安装/卸载和真实模型联网验收。

## 明确保留

用户数据、凭据、项目、工作区、日志、历史安装包、开发环境和资产未删除。
web/src 尚未证明与内联页面功能等价，因此以“未接入组件实现”明确隔离，不盲删。
空 storage 命名空间及 ui extra 作为兼容项保留。
本机归档不随 Git 分发，需要跨机器保留时请另外备份；旧提交仍保留历史源码。
